{
  "name": "gwinnett-index-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "esbuild src/index.ts --bundle --format=esm --platform=neutral --outfile=dist-test/index.js --log-level=warning && node test/mcp.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
